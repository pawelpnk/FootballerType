<div class="text-light d-flex flex-column align-items-center">
    <h3 class="text-light"><%= @match.first_team_name %> <%= @match.first_team_result.to_i %> : <%= @match.second_team_result.to_i %>  <%= @match.second_team_name %></h3>
    <h4>Gracz: <%= @match.player %>, Minuta: <%= @match.minutes.to_i %></h4>

    <div>
        <h4 class="my-3">Typy:</h4>
        <%= render 'shared/types' %>
  </div>

</div>

