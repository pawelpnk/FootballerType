 <nav class="navbar navbar-expand navbar-light my-0">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
            <%= link_to "Home", root_path, class: "nav-link text-light" %>
        </li>
        <li class="nav-item">
            <%= link_to "Spotkania", matches_path, class: "nav-link text-light" %>
        </li>
        <li class="nav-item">
            <%= link_to "Ranking", top_path, class: "nav-link text-light" %>
        </li>
    </ul>
    <ul class='navbar-nav ml-auto'>
        <% if user_signed_in? %>
            <li class="nav-item">
                <%= link_to "Wyloguj", destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
            </li>
        <% else %>
            <li class="nav-item">
                <%= link_to "Login", new_user_session_path, method: :get, class: "btn btn-outline-light btn-sm" %>
            </li>
            <li class="nav-item mx-3">
                <%= link_to "Rejestracja", new_user_registration_path, method: :get, class: "btn btn-outline-light btn-sm" %>
            </li>            
        <% end %>
    </ul>
    </div>
</nav>